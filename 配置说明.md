# Supabase 配置说明

## 第一步：注册并创建Supabase项目

1. 访问 https://supabase.com/
2. 点击 "Start your project" 注册账号（可用GitHub登录）
3. 点击 "New Project" 创建新项目
4. 填写项目信息：
   - **Name**: 项目名称，如 `juxiangyuan`
   - **Database Password**: 设置数据库密码（请记住）
   - **Region**: 选择 `Southeast Asia (Singapore)` 较快
5. 点击 "Create new project" 等待创建完成（约2分钟）

## 第二步：获取配置信息

1. 项目创建完成后，点击左侧菜单的 **⚙️ Project Settings**
2. 点击 **API** 选项卡
3. 你会看到以下信息：

### 需要复制的信息：

```
┌─────────────────────────────────────────────────────────┐
│  Project URL                                            │
│  ─────────────────────────────────────────────────────  │
│  https://xxxxxxxx.supabase.co        👈 复制这个URL     │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  Project API keys                                       │
│  ─────────────────────────────────────────────────────  │
│  anon (public)                                          │
│  eyJhbGciOiJIUzI1NiIs...                               │
│                                                         │
│  service_role (secret)     👈 复制这个key               │
│  eyJhbGciOiJIUzI1NiIs...                               │
└─────────────────────────────────────────────────────────┘
```

## 第三步：填写配置文件

打开项目根目录下的 `config.py` 文件，填入你的配置：

```python
# 【需要填写】Project URL
SUPABASE_URL = "https://你的项目ID.supabase.co"

# 【需要填写】service_role key
SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.你的密钥内容..."
```

## 第四步：初始化数据库

1. 在Supabase项目页面，点击左侧菜单的 **SQL Editor**
2. 点击 **New query**
3. **第一次执行**：复制 `database/schema.sql` 的全部内容，粘贴并点击 **Run**
4. **第二次执行**：复制 `database/init_data.sql` 的全部内容，粘贴并点击 **Run**

执行成功后，点击左侧 **Table Editor** 可以看到创建的表：
- tables (桌台表)
- dishes (菜品表)
- flavor_rounds (口味轮次表)
- flavor_options (口味选项表)
- bills (账单表)
- orders (订单表)
- order_items (订单明细表)

## 第五步：运行程序

```bash
# 安装依赖
pip install -r requirements.txt

# 运行程序
python main.py
```

## 常见问题

### Q: 提示 "数据库连接失败"
A: 检查 config.py 中的 URL 和 KEY 是否正确复制，注意不要有多余的空格

### Q: 提示 "表不存在"
A: 请确保在 SQL Editor 中执行了 schema.sql

### Q: 表里没有数据
A: 请确保在 SQL Editor 中执行了 init_data.sql

## 提交作业时需要的信息

根据课设要求，提交时需要提供：
1. **云数据库项目URL**: `https://你的项目ID.supabase.co`
2. **service_role key**: 在 Project Settings > API 中找到
